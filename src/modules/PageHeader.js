function PageHeader({ text }) {
    return (
        <header className="u-text-center">
            <h1 className="gradient-heading c-heading-bravo">{text}</h1>
        </header>
    );
}

export default PageHeader;